type Job @entity {
  id: ID!
  live: Boolean
  metadata: String
  owner: Bytes
  provider: Bytes
  rate: BigInt
  balance: BigInt
  lastSettled: BigInt
}

type Provider @entity {
  id: ID! #id same as owner address
  cp: String
  live: Boolean
}